{
    "name": "socket-even-client",
    "version": "0.0.1",
    "description": "check if a number is even",
    "scripts": {
        "test": "jest --no-watchman"
    },
    "jest": {
        "transform": {
            "^.+\\.jsx?$": "babel-jest"
        }
    },
    "devDependencies": {
        "@babel/preset-env": "^7.13.9",
        "babel-jest": "^26.6.3",
        "jest": "^26.6.3"
    }
}
