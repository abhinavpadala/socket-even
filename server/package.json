{
    "name": "socket-even",
    "version": "0.0.1",
    "description": "check if a number is even",
    "dependencies": {
        "express": "^4.17.1",
        "socket.io": "^3.1.2"
    },
    "scripts": {
        "test": "jest --no-watchman"
    },
    "devDependencies": {
        "jest": "^26.6.3"
    }
}
